class Content {
  Content(
      {this.id,
      this.title,
      this.description,
      this.mediaType,
      this.createdAt,
      this.mediaUrl});
  final String id, title, description, mediaType, createdAt, mediaUrl;
}
